#include "shell.h"
/**
 * main - Charge the configuration files for
 * the principal bucle
 * @argc: Entry arguments
 * @argv: Entry pointer
 * Return: SUCCESS
 */
int main(int argc, char **argv)
{
	main_loop();
	return (EXIT_SUCCESS);
}
