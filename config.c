#include "shell.h"
/**
 * main - Charge the configuration files for
 * the principal bucle
 * @argc: Entry arguments
 * @argv: Entry pointer
 * Return: SUCCESS
 */
void loop(void);

int main(void)
{
	loop();
	return (EXIT_SUCCESS);
}
